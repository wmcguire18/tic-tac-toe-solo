class Game {
  constructor(player1, player2, turn, result) {
    this.player1 = player1;
    this.player2 = player2;
    this.turn = player1;
    this.result = result;
  }
  //newGame
  //sets board and notifications to default for newGame, this.turn= player1;

  //toggleTurn
  //after player1 move (if no result) this.turn = player2
  //player2 has control of the board

  //findResult
  //determine if winning combination played OR no win POSSIBLE
};
